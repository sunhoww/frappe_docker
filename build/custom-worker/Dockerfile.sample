ARG GIT_BRANCH=version-12
ARG DOCKER_REGISTRY_PREFIX=frappe
FROM ${DOCKER_REGISTRY_PREFIX}/erpnext-worker:${GIT_BRANCH}

RUN install_app [app] [repo]

COPY ./redress.sh /redress
RUN chmod +x /redress && /redress
